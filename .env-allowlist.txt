# Legacy os.environ/os.getenv allowlist — EMPTY as of US3 (2026-09-15).
# Every read/write now goes through voiceai.core.environment.
# The gate fails closed on any new occurrence; never add entries here.
