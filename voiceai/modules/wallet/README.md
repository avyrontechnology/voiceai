# wallet

Credits + seed template catalog: singleton balance, top-ups with ledger entries,
template list/get/import. Canonical 10-file set (normalized in spec 0000):
literals in `constants.py`, guards in `exceptions.py`, pure projections in
`static_methods.py`, mappings in `helpers.py`, id generation in `utils.py`.

Owned by `squad-billing`. See `CONTRACT.md` and `RUNBOOK.md`.
